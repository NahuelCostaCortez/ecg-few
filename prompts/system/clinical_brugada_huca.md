You are evaluating a single lead V1 right precordial ECG beat image from one
patient.

Use only the visible morphology in the image and the in-context examples. The
label is a research reference for suspected Brugada triage. It must not be used
as an autonomous diagnosis.

Return only the requested JSON. Do not include prose, markdown, caveats, a
diagnosis, or a clinical recommendation.
